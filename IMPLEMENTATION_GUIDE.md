# دليل تنفيذ صفحة مساحة المدقق
## Implementation Guide for Auditor Workspace

---

## 📋 نظرة عامة

هذا الدليل يشرح كيفية تنفيذ صفحة مساحة المدقق في تطبيقك الفعلي بطريقة احترافية ومنظمة.

---

## 🎯 المتطلبات الأساسية

### 1. المكتبات المطلوبة:
\`\`\`bash
npm install lucide-react
npm install @radix-ui/react-checkbox
npm install @radix-ui/react-select
npm install @radix-ui/react-progress
\`\`\`

### 2. المكونات المطلوبة من shadcn/ui:
\`\`\`bash
npx shadcn@latest add button
npx shadcn@latest add card
npx shadcn@latest add badge
npx shadcn@latest add progress
npx shadcn@latest add textarea
npx shadcn@latest add label
npx shadcn@latest add select
npx shadcn@latest add checkbox
\`\`\`

---

## 🚀 خطوات التنفيذ

### الخطوة 1: نسخ الملف
1. انسخ محتوى ملف `EXPORT_auditor-workspace.tsx`
2. أنشئ ملف جديد في مشروعك: `components/auditor-workspace.tsx`
3. الصق المحتوى في الملف الجديد

### الخطوة 2: استيراد المكون
في صفحة لوحة التحكم الرئيسية:

\`\`\`tsx
import { AuditorWorkspace } from "@/components/auditor-workspace"

// في المكان المناسب:
<AuditorWorkspace />
\`\`\`

### الخطوة 3: ربط البيانات الحقيقية
استبدل البيانات الوهمية (Mock Data) بـ API calls حقيقية:

\`\`\`tsx
// بدلاً من:
const assignedTasks = [...]

// استخدم:
const { data: assignedTasks, isLoading } = useQuery({
  queryKey: ['assigned-tasks'],
  queryFn: () => fetch('/api/tasks/assigned').then(res => res.json())
})
\`\`\`

### الخطوة 4: ربط المطابقة القانونية
قم بربط وظيفة البحث بـ API الخاص بك:

\`\`\`tsx
const handleSearchCompliance = async () => {
  setIsSearching(true)
  try {
    const response = await fetch('/api/compliance/search', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ text: complianceText })
    })
    const results = await response.json()
    setComplianceResults(results)
  } catch (error) {
    console.error('Error:', error)
  } finally {
    setIsSearching(false)
  }
}
\`\`\`

---

## 🎨 التخصيص

### تغيير الألوان:
الصفحة تستخدم نظام ألوان Indigo/Cyan. لتغييره:

\`\`\`tsx
// ابحث عن:
bg-indigo-600 → bg-blue-600
text-indigo-400 → text-blue-400
border-indigo-500 → border-blue-500
\`\`\`

### تعديل النصوص:
جميع النصوص باللغة العربية. يمكنك تعديلها مباشرة في الكود.

---

## 🔧 الميزات الرئيسية

### 1. الكروت التحليلية (Stats Cards)
- عرض إحصائيات سريعة
- أيقونات ملونة
- أرقام ديناميكية

### 2. قائمة المهام
- عرض المهام المعينة
- تصنيف حسب الحالة والأولوية
- معلومات تفصيلية لكل مهمة

### 3. تفاصيل المهمة
- معلومات شاملة
- المستندات المرفقة
- إمكانية التحميل والرفع

### 4. قائمة الفحص (Checklist)
- تتبع التقدم
- عناصر قابلة للتحديد
- مؤشر التقدم

### 5. المطابقة القانونية الذكية ⭐
- إدخال النص
- بحث بالذكاء الاصطناعي
- عرض النتائج مع درجات التشابه
- تصنيف حسب القوة (قوية/متوسطة/مراجعة)

---

## 📊 هيكل البيانات المتوقع

### المهام (Tasks):
\`\`\`typescript
interface Task {
  id: string
  title: string
  department: string
  value: string
  status: string
  priority: string
  dueDate: string
  documents: number
  supplier?: string
  purchaseDate?: string
}
\`\`\`

### نتائج المطابقة (Compliance Results):
\`\`\`typescript
interface ComplianceResult {
  id: number
  law: string
  article: string
  similarity: number
  level: 'strong' | 'medium' | 'review'
  excerpt: string
}
\`\`\`

---

## ⚠️ ملاحظات مهمة

1. **الأداء**: استخدم React Query أو SWR لإدارة البيانات
2. **الأمان**: تأكد من تشفير البيانات الحساسة
3. **التحميل**: أضف حالات التحميل والأخطاء
4. **الاستجابة**: الصفحة responsive بالفعل، لكن اختبرها على أجهزة مختلفة

---

## 🐛 حل المشاكل الشائعة

### المشكلة: الأيقونات لا تظهر
**الحل**: تأكد من تثبيت `lucide-react`

### المشكلة: الألوان لا تعمل
**الحل**: تأكد من إعداد Tailwind CSS بشكل صحيح

### المشكلة: المكونات لا تعمل
**الحل**: تأكد من تثبيت جميع مكونات shadcn/ui المطلوبة

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. راجع الكود المصدري
2. تحقق من console للأخطاء
3. تأكد من تثبيت جميع المتطلبات

---

## ✅ قائمة التحقق النهائية

- [ ] تم نسخ الملف
- [ ] تم تثبيت المكتبات
- [ ] تم استيراد المكون
- [ ] تم ربط البيانات الحقيقية
- [ ] تم اختبار جميع الميزات
- [ ] تم التأكد من الاستجابة
- [ ] تم اختبار المطابقة القانونية

---

**تم إنشاء هذا الدليل بواسطة v0 - AuditOrbit Project**
